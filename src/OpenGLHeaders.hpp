#ifndef CUSTOM_OPENGL_HEADERS
#define CUSTOM_OPENGL_HEADERS

#include <OGL/OGL-3-3.hpp>

// OGL must always be first. Add line break so that clang-format doesnt
// reorder the order of them.
#include <GLFW/glfw3.h>

#endif
